# prophage_lysis v2.0.0 — pip requirements
# For conda users, use: conda env create -f environment.yaml
# For pip users:        pip install -r requirements.txt
#
# Note: HMMER and Pharokka must be installed separately via conda/bioconda
#   conda install -c bioconda hmmer pharokka

# Core scientific stack
biopython>=1.81
numpy>=1.24
scipy>=1.10
pandas>=2.0
networkx>=3.1

# Clustering and dimensionality reduction
umap-learn>=0.5.3
hdbscan>=0.8.33
scikit-learn>=1.3

# JIT compilation (CPU kernels)
numba>=0.57

# Visualization
matplotlib>=3.7

# Workflow engine
snakemake>=7.32

# CLI
typer>=0.9
pyyaml>=6.0

# HTTP + utilities
tqdm>=4.65
requests>=2.28

# ESM-2 — optional but needed for local backend
# fair-esm>=2.0.0
# torch>=2.0.0
